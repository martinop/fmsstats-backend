export enum ParticipantType {
  FREESTYLER = "FREESTYLER",
  JUDGE = "JUDGE",
}

export enum WinType {
  DIRECT = 'DIRECT',
  REPLICA = 'REPLICA',
}

export enum ModeWordType {
  EASY = 'EASY',
  HARD = 'HARD',
}